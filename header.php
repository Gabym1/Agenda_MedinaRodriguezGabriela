<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Agenda</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">

    <style>
        /* Fondo rosa para toda la página */
        body {
            background-color: #ffe6f0;
            font-family: Arial, sans-serif;
        }

        /* Encabezados rosas */
        .text-pink {
            color: #ff3399;
        }

        /* Botones rosas */
        .btn-pink {
            background-color: #ff66b2;
            color: white;
            border: none;
        }
        .btn-pink:hover {
            background-color: #ff3385;
            color: white;
        }

        /* Bordes rosas para tablas */
        .border-pink {
            border-color: #ff66b2 !important;
        }

        /* Filas de tabla alternadas en rosa */
        table.table-striped tbody tr:nth-of-type(odd) {
            background-color: #ffd9ec;
        }
        table.table-striped tbody tr:nth-of-type(even) {
            background-color: #ffe6f0;
        }

        /* Encabezado de tabla con degradado rosa */
        thead {
            background: linear-gradient(45deg, #ff66b2, #ff3399);
            color: white;
        }

        /* Sombra suave en la tabla */
        table {
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        /* Margen para el header */
        header {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <header class="py-3 mb-4 text-center">
    </header>
